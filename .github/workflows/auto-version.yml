name: Auto Version Bump (DISABLED)

# This workflow has been replaced by the PR-based version preparation workflow
# It is disabled to prevent conflicts with the new approach
#
# The new workflow files are:
# - .github/workflows/pr-version-prep.yml (prepares versions in PRs)
# - .github/workflows/main-tag-creation.yml (creates tags after PR merge)
# - .github/workflows/release.yml (creates releases from tags)

on:
  # Disabled - no triggers
  workflow_dispatch:
    inputs:
      force_run:
        description: 'Force run this disabled workflow (for emergency use only)'
        required: true
        default: 'false'

jobs:
  version-bump:
    runs-on: ubuntu-latest
    if: false  # Always skip this job
    permissions:
      contents: read

    steps:
    - name: Workflow Disabled Notice
      run: |
        echo "🚫 This workflow has been disabled and replaced with a PR-based approach"
        echo ""
        echo "📋 New Workflow Process:"
        echo "1. Create PR with conventional commits (feat:, fix:, etc.)"
        echo "2. pr-version-prep.yml analyzes commits and prepares version bump"
        echo "3. Version changes are included in the PR for review"
        echo "4. When PR is merged, main-tag-creation.yml creates release tag"
        echo "5. release.yml creates GitHub release with ZIP artifact"
        echo ""
        echo "✅ This approach respects branch protection rules and allows version review"
        echo ""
        echo "🔧 If you need to re-enable this workflow for emergency use:"
        echo "   - Update the 'if' condition to remove 'false'"
        echo "   - Ensure branch protection allows automation token to bypass PR requirements"
        exit 0
