name: Create Release

on:
  push:
    tags:
      - 'v*'

jobs:
  validate-release:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.VERSION }}
      is-prerelease: ${{ steps.version.outputs.IS_PRERELEASE }}
      should_create_release: ${{ steps.check_files.outputs.should_create_release }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Check if release is needed
      id: check_files
      run: |
        echo "🔍 Checking if release is needed based on changed files..."

        # Get the tag that triggered this workflow
        CURRENT_TAG="${GITHUB_REF#refs/tags/}"
        echo "🏷️  Current tag: $CURRENT_TAG"

        # Get the previous tag to compare against
        PREVIOUS_TAG=$(git describe --tags --abbrev=0 "$CURRENT_TAG^" 2>/dev/null || echo "")

        if [ -n "$PREVIOUS_TAG" ]; then
          echo "📋 Comparing changes since previous tag: $PREVIOUS_TAG"
          CHANGED_FILES=$(git diff --name-only "$PREVIOUS_TAG..$CURRENT_TAG")
        else
          echo "📋 No previous tags found, checking all files in current commit"
          CHANGED_FILES=$(git diff --name-only HEAD~1 2>/dev/null || git ls-files)
        fi

        echo "📁 Changed files since last release:"
        echo "$CHANGED_FILES"
        echo ""

        # Define ignore patterns (same as auto-version workflow)
        IGNORE_PATTERNS=(
          # Files managed by the version bump process itself
          "CHANGELOG.md"
          "package.json"
          "package-lock.json"
          "blaze-wooless.php"
          "blocks/package.json"

          # CI/CD and development tooling (no user impact)
          ".github/"
          "scripts/"
          "tests/"
          "test/"
          "bin/"

          # Internal documentation (no user impact)
          "docs/"
          "CONTRIBUTING.md"
          "TODO"
          "IMPLEMENTATION_SUMMARY.md"
          "VERSION_BUMP_BEHAVIOR_EXPLANATION.md"
          "CHANGELOG_VERSION_FIX.md"

          # Development configuration files
          "phpunit.xml"
          "jest.config.js"
          "github-workflows-tests.yml"
          "test.html"

          # Auto-generated files
          "composer.lock"
          "vendor/"
          "blocks/yarn.lock"
          "blocks/package-lock.json"

          # Git and editor files
          ".gitignore"
          ".editorconfig"
          ".DS_Store"

          # AI assistant and editor configuration
          ".augment/"
          ".claude/"
          ".vscode/"
        )

        # Check if any changed file should trigger release creation
        SHOULD_CREATE_RELEASE="false"

        while IFS= read -r file; do
          [ -z "$file" ] && continue

          FILE_SHOULD_BE_IGNORED="false"

          # Check against each ignore pattern
          for pattern in "${IGNORE_PATTERNS[@]}"; do
            # Handle directory patterns (ending with /)
            if [[ "$pattern" == */ ]]; then
              if [[ "$file" == "$pattern"* ]]; then
                FILE_SHOULD_BE_IGNORED="true"
                break
              fi
            # Handle exact file matches and wildcard patterns
            elif [[ "$file" == "$pattern" ]] || [[ "$file" == *"$pattern" ]]; then
              FILE_SHOULD_BE_IGNORED="true"
              break
            fi
          done

          # If this file should NOT be ignored, we need to create release
          if [ "$FILE_SHOULD_BE_IGNORED" = "false" ]; then
            echo "✅ File '$file' requires release creation"
            SHOULD_CREATE_RELEASE="true"
          else
            echo "⏭️  File '$file' is ignored for release creation"
          fi
        done <<< "$CHANGED_FILES"

        echo ""
        if [ "$SHOULD_CREATE_RELEASE" = "true" ]; then
          echo "🚀 Release creation needed - non-ignored files were changed"
        else
          echo "⏭️  Release creation skipped - only ignored files were changed"
        fi

        echo "should_create_release=$SHOULD_CREATE_RELEASE" >> $GITHUB_OUTPUT

    - name: Setup Node.js
      if: steps.check_files.outputs.should_create_release == 'true'
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      if: steps.check_files.outputs.should_create_release == 'true'
      run: npm install

    - name: Extract and validate version from tag
      if: steps.check_files.outputs.should_create_release == 'true'
      id: version
      run: |
        VERSION=${GITHUB_REF#refs/tags/v}
        echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
        echo "📦 Release version: $VERSION"

        # Validate semantic version format
        if ! node -e "
          const { isValidSemver } = require('./scripts/semver-utils');
          if (!isValidSemver('$VERSION')) {
            console.error('❌ Invalid semantic version format: $VERSION');
            process.exit(1);
          }
          console.log('✅ Valid semantic version format');
        "; then
          exit 1
        fi

        # Check if it's a prerelease
        if echo "$VERSION" | grep -qE "\-"; then
          echo "IS_PRERELEASE=true" >> $GITHUB_OUTPUT
          echo "🚧 Pre-release version detected"
        else
          echo "IS_PRERELEASE=false" >> $GITHUB_OUTPUT
          echo "🚀 Stable release version"
        fi

    - name: Validate YAML syntax
      if: steps.check_files.outputs.should_create_release == 'true'
      run: |
        echo "🔍 Validating YAML syntax in workflow files..."
        python3 -c "
        import yaml
        import sys
        try:
            with open('.github/workflows/release.yml', 'r') as f:
                yaml.safe_load(f)
            print('✅ YAML syntax is valid')
        except yaml.YAMLError as e:
            print(f'❌ YAML syntax error: {e}')
            sys.exit(1)
        "

    - name: Validate version consistency
      if: steps.check_files.outputs.should_create_release == 'true'
      run: |
        echo "🔍 Validating version consistency across files..."
        node scripts/validate-version.js --verbose
        if [ $? -ne 0 ]; then
          echo "❌ Version validation failed"
          exit 1
        fi
        echo "✅ Version validation passed"

  build-and-release:
    needs: validate-release
    runs-on: ubuntu-latest
    if: needs.validate-release.outputs.should_create_release == 'true'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.1'
        extensions: mbstring, intl
        tools: composer

    - name: Install dependencies
      run: |
        echo "📦 Installing Node.js dependencies..."
        npm install

        echo "📦 Installing PHP dependencies..."
        composer install --no-dev --optimize-autoloader --no-interaction

    - name: Build blocks
      run: |
        echo "🔨 Building Gutenberg blocks..."
        cd blocks
        npm install
        npm run build

        echo "✅ Blocks built successfully"
        ls -la build/

    - name: Validate build artifacts
      run: |
        echo "🔍 Validating build artifacts..."

        # Check if blocks were built
        if [ ! -d "blocks/build" ] || [ -z "$(ls -A blocks/build)" ]; then
          echo "❌ Blocks build directory is missing or empty"
          exit 1
        fi

        # Check main plugin file
        if [ ! -f "blaze-wooless.php" ]; then
          echo "❌ Main plugin file is missing"
          exit 1
        fi

        # Validate PHP syntax
        echo "🔍 Validating PHP syntax..."
        find . -name "*.php" -not -path "./vendor/*" -not -path "./node_modules/*" -exec php -l {} \; > /dev/null
        if [ $? -ne 0 ]; then
          echo "❌ PHP syntax validation failed"
          exit 1
        fi

        echo "✅ Build artifacts validated successfully"

    - name: Create plugin ZIP
      run: |
        VERSION="${{ needs.validate-release.outputs.version }}"
        ZIP_NAME="blazecommerce-wp-plugin-$VERSION.zip"

        echo "📦 Creating plugin ZIP: $ZIP_NAME"

        # Create a temporary directory for the plugin
        TEMP_DIR="/tmp/blazecommerce-wp-plugin"
        mkdir -p "$TEMP_DIR"

        # Copy plugin files (exclude development files)
        echo "📋 Copying plugin files..."
        rsync -av --exclude-from='.github/workflows/.zipignore' . "$TEMP_DIR/"

        # Verify essential files are included
        echo "🔍 Verifying essential files in ZIP..."
        ESSENTIAL_FILES=("blaze-wooless.php" "blocks/build")
        for file in "${ESSENTIAL_FILES[@]}"; do
          if [ ! -e "$TEMP_DIR/$file" ]; then
            echo "❌ Essential file/directory missing from ZIP: $file"
            exit 1
          fi
        done

        # Create the ZIP file
        cd /tmp
        zip -r "$ZIP_NAME" blazecommerce-wp-plugin/ -q

        # Verify ZIP was created and has reasonable size
        if [ ! -f "$ZIP_NAME" ]; then
          echo "❌ ZIP file was not created"
          exit 1
        fi

        ZIP_SIZE=$(stat -f%z "$ZIP_NAME" 2>/dev/null || stat -c%s "$ZIP_NAME")
        if [ "$ZIP_SIZE" -lt 10000 ]; then
          echo "❌ ZIP file seems too small ($ZIP_SIZE bytes)"
          exit 1
        fi

        echo "✅ ZIP file created successfully ($ZIP_SIZE bytes)"

        # Move ZIP back to workspace
        mv "$ZIP_NAME" "$GITHUB_WORKSPACE/"

    - name: Generate enhanced release notes
      id: release_notes
      run: |
        VERSION="${{ needs.validate-release.outputs.version }}"
        echo "📝 Generating release notes for version $VERSION..."

        # Initialize release notes
        RELEASE_NOTES=""

        # Extract changelog for this version if it exists
        if [ -f CHANGELOG.md ]; then
          echo "📋 Extracting changelog for version $VERSION..."

          # Get content between this version and next version header
          CHANGELOG_CONTENT=$(awk "/^## \[$VERSION\]/{flag=1; next} /^## \[/{flag=0} flag" CHANGELOG.md | sed '/^$/d' | head -50)

          if [ -n "$CHANGELOG_CONTENT" ]; then
            # Note: Using string concatenation instead of heredoc (cat << EOF)
            # to avoid YAML parsing issues. Heredoc syntax can confuse YAML parsers
            # when used within shell scripts in GitHub Actions workflows.
            # This approach was implemented based on Claude AI's review recommendations.
            RELEASE_NOTES="## What's Changed"$'\n\n'"$CHANGELOG_CONTENT"$'\n\n'"---"
          fi
        fi

        # Build release notes sections using variables for better readability
        # (Improved based on Claude AI's maintainability recommendations)

        # Installation instructions section
        INSTALL_SECTION="## 📦 Installation"$'\n\n'"1. Download the \`blazecommerce-wp-plugin-$VERSION.zip\` file below"$'\n'"2. In your WordPress admin, go to **Plugins > Add New > Upload Plugin**"$'\n'"3. Choose the downloaded ZIP file and click **Install Now**"$'\n'"4. Activate the plugin after installation"

        # Requirements section
        REQUIREMENTS_SECTION="## 🔧 Requirements"$'\n\n'"- WordPress 5.0 or higher"$'\n'"- PHP 7.4 or higher"$'\n'"- WooCommerce plugin"

        # Documentation section
        DOCS_SECTION="## 📚 Documentation"$'\n\n'"For setup and configuration instructions, visit our [documentation](https://github.com/blaze-commerce/blazecommerce-wp-plugin/tree/main/docs)."

        # Support section
        SUPPORT_SECTION="## 🐛 Support"$'\n\n'"If you encounter any issues, please [create an issue](https://github.com/blaze-commerce/blazecommerce-wp-plugin/issues) on GitHub."

        # Combine all sections into final release notes
        RELEASE_NOTES="$RELEASE_NOTES"$'\n\n'"$INSTALL_SECTION"$'\n\n'"$REQUIREMENTS_SECTION"$'\n\n'"$DOCS_SECTION"$'\n\n'"$SUPPORT_SECTION"

        # Save release notes to output
        echo "RELEASE_NOTES<<EOF" >> $GITHUB_OUTPUT
        echo "$RELEASE_NOTES" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT

        echo "✅ Release notes generated successfully"

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ github.ref_name }}
        name: "🚀 Release ${{ needs.validate-release.outputs.version }}"
        body: ${{ steps.release_notes.outputs.RELEASE_NOTES }}
        files: |
          blazecommerce-wp-plugin-${{ needs.validate-release.outputs.version }}.zip
        draft: false
        prerelease: ${{ needs.validate-release.outputs.is-prerelease == 'true' }}
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.BC_GITHUB_TOKEN }}

    - name: Release summary
      run: |
        VERSION="${{ needs.validate-release.outputs.version }}"
        IS_PRERELEASE="${{ needs.validate-release.outputs.is-prerelease }}"

        echo "🎉 Release completed successfully!"
        echo "📦 Version: $VERSION"
        echo "🚧 Pre-release: $IS_PRERELEASE"
        echo "📁 Asset: blazecommerce-wp-plugin-$VERSION.zip"
        echo "🔗 Release URL: https://github.com/${{ github.repository }}/releases/tag/v$VERSION"

  workflow-summary:
    needs: validate-release
    runs-on: ubuntu-latest
    if: always()

    steps:
    - name: Workflow Summary
      run: |
        echo "📋 Create Release Workflow Summary"
        echo "=================================="

        TAG_NAME="${GITHUB_REF#refs/tags/}"
        echo "🏷️  Triggered by tag: $TAG_NAME"

        if [ "${{ needs.validate-release.outputs.should_create_release }}" = "true" ]; then
          echo "✅ Release was processed"

          # Check if build-and-release job was successful
          if [ "${{ needs.validate-release.result }}" = "success" ]; then
            VERSION="${{ needs.validate-release.outputs.version }}"
            IS_PRERELEASE="${{ needs.validate-release.outputs.is-prerelease }}"

            echo "📦 Version: $VERSION"
            echo "🚧 Pre-release: $IS_PRERELEASE"
            echo "📁 Asset: blazecommerce-wp-plugin-$VERSION.zip"
            echo "🔗 Release URL: https://github.com/${{ github.repository }}/releases/tag/$TAG_NAME"
            echo "🎉 Release created successfully!"
          else
            echo "❌ Release processing failed during validation"
            echo "   Check the validate-release job logs for details"
          fi
        else
          echo "⏭️  Release creation was skipped"
          echo "📁 Reason: Only ignored files were changed since last release"
          echo "ℹ️  This is normal behavior - no release needed for:"
          echo "   • Documentation changes"
          echo "   • CI/CD configuration updates"
          echo "   • Development tooling changes"
          echo "   • Auto-generated files"
          echo "   • Version bump commits"
          echo ""
          echo "✅ Workflow completed successfully (no action required)"
          echo "💡 The tag $TAG_NAME remains available but no GitHub release was created"
        fi
