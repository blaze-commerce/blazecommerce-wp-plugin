{
  "name": "blazecommerce-wp-plugin",
  "version": "1.8.0",
  "description": "The official plugin that integrates your site with the Blaze Commerce service.",
  "main": "blaze-wooless.php",
  "scripts": {
    "version:patch": "npm version patch --no-git-tag-version && npm run update-plugin-version",
    "version:minor": "npm version minor --no-git-tag-version && npm run update-plugin-version",
    "version:major": "npm version major --no-git-tag-version && npm run update-plugin-version",
    "version:prerelease": "npm version prerelease --no-git-tag-version && npm run update-plugin-version",
    "update-plugin-version": "node scripts/update-version.js",
    "validate-version": "node scripts/validate-version.js",
    "validate-version:verbose": "node scripts/validate-version.js --verbose",
    "build": "cd blocks && npm run build",
    "build:blocks": "cd blocks && npm run build",
    "release": "npm run build && node scripts/create-release.js",
    "release:dry-run": "npm run build && node scripts/create-release.js --dry-run",
    "changelog": "node scripts/update-changelog.js",
    "changelog:dry-run": "node scripts/update-changelog.js --dry-run",
    "changelog:verbose": "node scripts/update-changelog.js --verbose",
    "changelog:technical": "node scripts/update-changelog.js --technical",
    "changelog:grouped": "node scripts/update-changelog.js --group-by-scope",
    "changelog:preview": "node scripts/update-changelog.js --dry-run --verbose --group-by-scope",
    "version:check": "node -e \"console.log('Current version:', require('./package.json').version)\"",
    "version:validate-all": "npm run validate-version && npm run build && echo '✅ All validations passed'",
    "test:version-system": "node scripts/test-version-system.js",
    "test:workflow-scenarios": "node scripts/test-workflow-scenarios.js",
    "test:validation-flags": "node scripts/test-validation-flags.js",
    "test:version-complete": "npm run test:version-system && npm run test:workflow-scenarios && npm run test:validation-flags",
    "_comment": "CLAUDE AI REVIEW: Fixed duplicate prepare-release script from comment #3060465549",
    "prepare-release": "npm run validate-version && npm run build && npm run changelog && echo '🚀 Release preparation complete'",
    "prepare-release:quick": "npm run changelog && npm run build && git add .",
    "validate-docs": "node scripts/validate-docs.js",
    "docs:validate": "node scripts/validate-docs.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/blaze-commerce/blazecommerce-wp-plugin.git"
  },
  "keywords": [
    "wordpress",
    "plugin",
    "woocommerce",
    "blazecommerce",
    "ecommerce"
  ],
  "author": "Blaze Commerce",
  "license": "GPL-2.0-or-later",
  "bugs": {
    "url": "https://github.com/blaze-commerce/blazecommerce-wp-plugin/issues"
  },
  "homepage": "https://www.blazecommerce.io",
  "devDependencies": {
    "jest": "^29.7.0",
    "js-yaml": "^4.1.0",
    "semver": "^7.5.4"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
